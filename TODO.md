# Available Copies Bug Fix Plan\n\n**Information Gathered:**\n- Frontend sets `availableCopies: 0` in payload\n- Backend BookController sets `availableCopies = totalCopies` if null\n- Book.java entity has `availableCopies` nullable=false, no default\n- Issue: frontend explicitly sends 0, backend logic skips setting\n- Borrow/return logic in other controllers/services needs verification\n- Status calculated frontend from availableCopies > 0\n\n**Plan:**\n1. Frontend manage-books.ts: Change payload to `availableCopies: formValue.totalCopies`\n2. Backend BookController.java addBook(): Always force `availableCopies = totalCopies`\n3. Update BookController updateBook(): `Math.min(availableCopies, newTotalCopies)` safety\n4. Backend BookRepository/Service: No changes needed\n5. Frontend book.model.ts/service.ts: Good\n6. Migration SQL for existing books: UPDATE books SET availableCopies = totalCopies WHERE availableCopies = 0;\n\n**Dependent Files:**\n- BookNest/frontend/src/app/pages/admin/manage-books/manage-books.ts\n- BookNest/backend/src/main/java/com/booknest/backend/controller/BookController.java\n\n**Followup:**\n1. Apply edits\n2. Run migration SQL\n3. Test add book → available = total\n4. Verify borrow approval decreases available\n\nConfirm plan?
